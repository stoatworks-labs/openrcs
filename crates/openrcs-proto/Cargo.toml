[package]
name = "openrcs-proto"
description = "Wire protocol for Analog Way Midra and LiveCore series video processors"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["std"]
std = []

[[example]]
name = "probe"
required-features = ["std"]
